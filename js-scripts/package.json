{
  "name": "js-scripts",
  "version": "1.0.0",
  "description": "Scripts in JavaScript to search a repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "swc index.ts --config-file .swcrc --out-dir .",
    "clean": " rm -f src/*.js src/**/*.js"
  },
  "author": "Elias Rangel",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^22.7.0",
    "dependency-cruiser": "^16.4.2",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  }
}
